#include <stdio.h>


int main(int argc, char const *argv[])
{
    int elm, a = 0, b = 1, nextnum = 0;
    printf("\nenter the number of elements to print: ");
    for (int i = 0; i < elm; i++)
    {
        printf("%d ", nextnum);
        nextnum = 
    }
    
}
